# Backend Configuration
# Il proxy in vite.config.ts è configurato per puntare al backend remoto
# Se vuoi cambiare il backend, modifica il 'target' in vite.config.ts

# Esempio per backend locale:
# target: "http://127.0.0.1:8000"

# Esempio per backend remoto (attualmente configurato):
# target: "http://34.61.165.2:8000"
